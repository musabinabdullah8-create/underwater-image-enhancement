Run the following command inside the project folder:

powershell
venv\Scripts\activate
You should immediately see (venv) appear at the beginning of the prompt.

Then run the app:

powershell
streamlit run app.py